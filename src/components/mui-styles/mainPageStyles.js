import { styled } from '@mui/system';

export const StyledMainPage = styled('div', {
  name: 'StyledMainPage',
})({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'center',
  width: '100%',
});
